exports.add = (a, b) => a + b

exports.multiply = (a, b) => a * b

// we can also assign the new key and value directly to export object and we will get the same key value structure as module-two but we did't use this very oftern